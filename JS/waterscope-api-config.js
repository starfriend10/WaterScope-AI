/**
 * Set to your public retrieval API base URL (no trailing slash).
 * Leave empty when the Flask app serves pages from the same origin.
 */
window.WATERSCOPE_API = "https://environmentalai-waterscopeai-search.hf.space";
